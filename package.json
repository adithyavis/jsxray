{
  "name": "jsxray-workspace",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "engines": {
    "node": ">=20.11"
  },
  "scripts": {
    "build": "tsc -b packages/core packages/providers packages/cli && npm --workspace @jsxray/viewer run build && npm --workspace @jsxray/viewer run build:single",
    "clean": "tsc -b --clean packages/core packages/providers packages/cli",
    "typecheck": "tsc -b --pretty packages/core packages/providers packages/cli && tsc -p packages/viewer/tsconfig.json --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "jsxray": "tsx packages/cli/src/bin.ts",
    "smoke": "node scripts/smoke.mjs",
    "smoke:clone": "node scripts/smoke.mjs --clone"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "playwright": "^1.62.1",
    "tsx": "^4.20.0",
    "typescript": "^7.0.2",
    "vitest": "^4.1.10"
  }
}
